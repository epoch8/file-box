{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "mypy check globally",
            "type": "shell",
            "command": "./venv/bin/mypy --ignore-missing-imports --follow-imports=skip -p file_box"
        }
    ]
}